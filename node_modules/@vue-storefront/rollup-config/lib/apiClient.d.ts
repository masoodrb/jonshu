import { OutputBundle, OutputOptions } from 'rollup';
export declare function generateServerConfig(pkg: any): {
    input: string;
    output: {
        file: any;
        format: string;
        sourcemap: boolean;
    }[];
    external: string[];
    plugins: (import("rollup").Plugin | {
        name: string;
        generateBundle: (options: OutputOptions, bundle: OutputBundle) => void;
    })[];
};
