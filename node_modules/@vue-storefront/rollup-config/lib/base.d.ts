export declare function generateBaseConfig(pkg: any): {
    input: string;
    output: {
        file: any;
        format: string;
        sourcemap: boolean;
    }[];
    external: string[];
    plugins: import("rollup").Plugin[];
};
