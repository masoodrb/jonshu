import { computed as s, unref as c } from "vue";
import { onClickOutside as l, onKeyStroke as d } from "@vueuse/core";
import { offset as u, shift as a, flip as w } from "@floating-ui/vue";
import { usePopover as O } from "../usePopover/usePopover.mjs";
function C(t) {
  const { onClose: o, placement: r = "bottom", middleware: n, isOpen: p, ...f } = t, { floatingRef: i, referenceRef: e, style: m } = O({
    placement: r,
    middleware: s(() => c(n) || [u(8), a(), w()]),
    isOpen: p,
    ...f
  });
  return l(e, o), d("Escape", o, { target: e }), { floatingRef: i, referenceRef: e, style: m };
}
export {
  C as useDropdown
};
